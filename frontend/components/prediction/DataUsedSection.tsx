"use client";

import React from "react";
import { Database, Clock, Calendar, Cpu, Server } from "lucide-react";
import { Card } from "@/components/common/Card";
import { ModelStatusResponse, LiveMarketData, CryptoPredictionResponse } from "@/lib/api";

interface DataUsedSectionProps {
  ticker: string;
  modelStatus: ModelStatusResponse | null;
  liveData: LiveMarketData | null;
  prediction: CryptoPredictionResponse | null;
}

export function DataUsedSection({
  ticker,
  modelStatus,
  liveData,
  prediction,
}: DataUsedSectionProps) {
  const daysAvailable = modelStatus?.available_history_days || prediction?.available_history_days || 365;
  const lookback = modelStatus?.lookback_days || prediction?.sequence_length || 60;
  const lastDataDate = prediction?.last_data_date || "Latest daily bar";
  const forecastDate = prediction?.forecast_date || "Next trading day";

  const marketSource = (prediction as any)?.sources?.market || liveData?.source || "Yahoo Finance";
  const historicalSource = (prediction as any)?.sources?.historical || "Yahoo Finance (Daily OHLCV Series)";
  const modelId = prediction?.model_name || (prediction as any)?.sources?.model || `${ticker}_lstm.keras`;

  const marketUpdated = liveData?.timestamp
    ? new Date(liveData.timestamp).toLocaleTimeString()
    : "Live Feed";

  return (
    <Card variant="bordered" className="space-y-6">
      <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-2 pb-2 border-b border-slate-800">
        <div className="flex items-center space-x-2">
          <Database className="w-5 h-5 text-indigo-400" />
          <h2 className="text-xl font-bold text-white">WHERE DID THIS ANALYSIS COME FROM?</h2>
        </div>
        <span className="text-xs font-mono text-slate-400">Analysis Based On Validated Telemetry</span>
      </div>

      <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-3 text-xs">
        <div className="bg-slate-950/70 border border-slate-800 p-3 rounded-xl space-y-1">
          <span className="text-[10px] text-slate-500 uppercase block font-semibold">Market Feed Provider</span>
          <span className="font-mono font-bold text-emerald-400 block text-xs truncate">{marketSource}</span>
        </div>

        <div className="bg-slate-950/70 border border-slate-800 p-3 rounded-xl space-y-1">
          <span className="text-[10px] text-slate-500 uppercase block font-semibold">Historical Feed</span>
          <span className="font-mono font-bold text-slate-200 block text-xs truncate">{historicalSource}</span>
        </div>

        <div className="bg-slate-950/70 border border-slate-800 p-3 rounded-xl space-y-1">
          <span className="text-[10px] text-slate-500 uppercase block font-semibold">Historical Days</span>
          <span className="font-mono font-bold text-slate-200 block text-sm">{daysAvailable} Days</span>
        </div>

        <div className="bg-slate-950/70 border border-slate-800 p-3 rounded-xl space-y-1">
          <span className="text-[10px] text-slate-500 uppercase block font-semibold">Lookback Window</span>
          <span className="font-mono font-bold text-blue-400 block text-sm">{lookback} Days</span>
        </div>

        <div className="bg-slate-950/70 border border-slate-800 p-3 rounded-xl space-y-1">
          <span className="text-[10px] text-slate-500 uppercase block font-semibold">Forecast Horizon</span>
          <span className="font-mono font-bold text-indigo-400 block text-sm">1 Day</span>
        </div>

        <div className="bg-slate-950/70 border border-slate-800 p-3 rounded-xl space-y-1">
          <span className="text-[10px] text-slate-500 uppercase block font-semibold">Trained AI Model</span>
          <span className="font-mono font-bold text-amber-400 block text-xs truncate">{modelId}</span>
        </div>
      </div>

      {/* WHAT THE AI USED CARD */}
      <div className="bg-slate-950/80 border border-slate-800 rounded-2xl p-4 space-y-3">
        <h3 className="text-sm font-bold text-white flex items-center space-x-2">
          <Cpu className="w-4 h-4 text-blue-400" />
          <span>What the AI Used</span>
        </h3>
        <p className="text-xs text-slate-300 leading-relaxed">
          The forecast was generated by feeding the selected cryptocurrency's past {lookback} consecutive daily closing prices ($USD) into a trained 2-layer Stacked LSTM (Long Short-Term Memory) neural network. The neural network learns sequential price momentum and non-linear patterns over time.
        </p>
      </div>

      {/* WHAT THIS FORECAST DOES NOT KNOW CARD */}
      <div className="bg-rose-500/5 border border-rose-500/20 rounded-2xl p-4 space-y-2">
        <h3 className="text-xs font-bold text-rose-300 uppercase tracking-wider">What This Forecast Does Not Know</h3>
        <ul className="text-xs text-slate-400 space-y-1 list-disc list-inside">
          <li>Unannounced macroeconomic news, central bank interest rate decisions, or regulatory policies.</li>
          <li>Real-time social media sentiment, breaking news events, or exchange order-book depth.</li>
          <li>On-chain whale wallet movements or sudden external crypto market shocks.</li>
        </ul>
      </div>
    </Card>
  );
}
